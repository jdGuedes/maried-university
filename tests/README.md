# Tests

Testes unitarios, integracao, RLS e fluxos criticos.

## SQL

- `sql/spec_001_core_identity_isolation.sql`: valida isolamento entre dois usuarios e dois tenants para a SPEC-001.

Os testes SQL devem ser executados em banco local ou descartavel. Nao executar contra producao.
